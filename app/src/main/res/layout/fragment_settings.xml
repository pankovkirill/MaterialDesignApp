<?xml version="1.0" encoding="utf-8"?>
<androidx.coordinatorlayout.widget.CoordinatorLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    tools:context=".view.SettingsFragment">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:paddingTop="@dimen/xx_padding">

        <com.google.android.material.textview.MaterialTextView
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:gravity="left"
            android:paddingStart="@dimen/x_padding"
            android:text="@string/decoration"
            android:textColor="?attr/textColorChangeTheme" />

        <com.google.android.material.textview.MaterialTextView
            android:id="@+id/change_theme"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:gravity="right"
            android:paddingEnd="@dimen/x_padding"
            android:textColor="?attr/textColorChangeTheme"
            tools:text="@string/custom_theme" />

    </LinearLayout>

    <include layout="@layout/bottom_sheet_settings" />
</androidx.coordinatorlayout.widget.CoordinatorLayout>